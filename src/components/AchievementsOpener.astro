---
// STUB — replaced by the openers builder. Props: entries, total, firstYear, lastYear. Renders the chapter header for now.
import ChapterHeader from './ChapterHeader.astro';
interface Props { entries?: unknown[]; total?: number; firstYear?: number; lastYear?: number }
---
<ChapterHeader slug="achievements"><slot /></ChapterHeader>
