---
// STUB — replaced by the hero agent. Interface: no props. Renders the full-height home hero (wordmark + graph).
import { PERSON } from '../site.config.ts';
---
<section class="hero container"><h1 class="display" style="font-size:var(--t-hero);line-height:.88;letter-spacing:-.045em;text-transform:uppercase">{PERSON.firstName}<br />{PERSON.lastName}</h1></section>
